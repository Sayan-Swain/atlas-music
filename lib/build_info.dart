/// Release stamp, shown in Profile → About. Bump on every release
/// build. Proves which APK is installed when debugging reports
/// like "nothing changed" (stale install vs real regression).
const buildStamp = 'b53-media-notif';
